<project name="clojure" default="jar">

  <description>
	
  </description>

  <property name="src" location="src"/>
  <property name="build" location="classes"/>
  <property name="swingstates_jar" location="swingstates.jar"/>

  <target name="init">
    <tstamp/>
    <mkdir dir="${build}"/>
  </target>

  <target name="compile_java" depends="init"
          description="Compile Java sources.">
    <javac srcdir="${src}" destdir="${build}"
	   includeJavaRuntime="yes"
           debug="true" target="1.5"/>
  </target>

  <target name="jar" depends="compile_java"
          description="Create jar file.">
    <jar jarfile="${swingstates_jar}" basedir="${build}"/>
  </target>

  <target name="clean"
          description="Remove autogenerated files and directories.">
    <delete dir="${build}"/>
  </target>

</project>
